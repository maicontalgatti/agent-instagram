# Títulos já postados (evita repetição local)
used_titles.txt

# Índice da rotação notícias / curiosidades / tendências
post_mode_index.txt

# Estado editorial (URLs já postadas)
data/editorial_state.json
